# RFCs — FlowFlow

| ID | Title | Status | Finalized | Recommendation |
|----|-------|--------|-----------|----------------|
| 0001 | [Backup, export & restore des données FlowFlow](./0001-data-backup-export/RFC.md) | Review | — | — |
| 0002 | [Import audio dans une note pour transcription](./0002-audio-import-transcription/RFC.md) | Review | — | Implémenté |
| 0003 | [smart note-driven reminders](./0003-smart-note-driven-reminders/RFC.md) | Accepted | 2026-06-01 | Alt 4 hybride - EventKit Reminders + UN fallback (confidence medium) |
| 0004 | [Synchronisation multi-appareils (LAN, sans serveur)](./0004-multidevice-sync/RFC.md) | Accepted | 2026-06-09 | Alt 4 - sync LAN pair-à-pair: rusqlite + version vector + tombstones + Noise/PSK + BLOB vecteurs; zéro perte, rien hors des appareils; audio (fichiers) descopé v1, transcription seule (confidence medium, 2 spikes à lever) |
| 0005 | [Pluggable transcription providers with local Whisper models](./0005-pluggable-transcription-providers-with-local-whisper-models/RFC.md) | Accepted | 2026-06-11 | Alt 2 - façade enum TranscriptionClient (pattern LlmClient): Soniox inchangé + WhisperLocal (whisper-rs, spike iOS validé) + catalogue 5 modèles ggml au choix de l'utilisateur (sha256 épinglés); benchmark iPhone T01 = gate de merge (confidence medium-high) |
| 0006 | [Note threads](./0006-note-threads/RFC.md) | Accepted | 2026-06-18 | Alt 2 - nouvelle table `threads` + `notes.thread_id` (FK nullable, 1:N, ordre chronologique): fil titré sous un thème, timeline inline, "Ajouter au thread" en place, scope RAG par thread (allow-list #42), un seul `ChatScope` enum; V13 + catalogue sync. Revue adversariale 3 agents: 3 BLOCKERs corrigés (install trigger à l'upgrade, convergence delete sur le peer, scope unifié). #54 (confidence high) |
| 0008 | [Connecteurs MCP externes via backend broker OAuth (agent hybride on-device)](./0008-mcp-connectors-oauth-broker-backend/RFC.md) | Accepted | 2026-06-19 | Alt 2 - l'agent rig on-device devient client MCP (feature `rmcp`, Streamable HTTP); backend mince (repo privé séparé, Rust/axum) = broker OAuth/BFF (client secrets + refresh tokens côté serveur) + proxy `/mcp/{server}`; slice mince = 1 serveur GitHub MCP self-hosté de bout en bout depuis le chat; monétisation déférée (RFC séparé). 2 keystones research-confirmés (rig `rmcp`, `objc2-authentication-services`), 3 spikes-gate (T00a/T00b/T02b). Revue adversariale 2 agents: 6 BLOCKERs corrigés (nonce replay, SSRF/open-proxy, isolation tenant, identity recovery, serveur GitHub épinglé self-host, token broker testé). (confidence medium) |
| 0012 | [M1.16 console agent: validate / simulate / sign / publish lifecycle + admin test harness](./0012-console-agent-lifecycle/RFC.md) | Accepted | 2026-06-25 | Alt 4 deterministic-first, CONSOLE SEULE (Revision 3, après 2 tours de revue + décision §12). Construit sur le gate backend existant; parité avec le device garantie par un corpus de conformité (vecteurs golden rejoués des deux côtés) - pas de crate partagé, pas de build iOS modifié, pas de CI à monter. Backend seul (marketplace-flowflow): réconcilier le gate à la sémantique per-resource du device + porter le FSM (T00, gardé par le corpus), migration V7 (ADD COLUMN + 1 relax), endpoints draft/validate/publish (rapport JSON synchrone), publish default-deny lié au digest validé; corpus de tests stocké backend-side. Le crate partagé (dedup compile-time) est scindé vers le RFC 0013. (confidence medium-high) |
| 0014 | [Platform web app: accounts, roles, and agent management](./0014-platform-web-app-accounts-roles-agent-management/RFC.md) | Accepted | 2026-06-26 | Alt B (plateforme complète, 5 phases). Compte web par passkey/WebAuthn (email = champ profil, pas de mot de passe), 3 rôles admin/développeur/utilisateur réutilisant les overrides existants, workflow de demande d'accès tracé, builder d'agents visuel par étapes (éditeur de chaînes -> canvas React Flow) compilant vers le FSM gouverné existant; exécution sur l'appareil via la palette + et les mots-clés paramétrés, sur les propres connecteurs de l'utilisateur; modèle gpt-5.4-mini + gpt-5.4-nano (champ par-agent câblé). Assume de revenir sur le verrou zéro-PII (adouci par passkey). Revue adversariale 2 agents: 7 BLOCKERs corrigés en Revision 2 (passkey, lien de compte unique, garde d'approbation admin avant signature, gouvernance des colonnes à l'armement, effacement RGPD, sessions maison, tâches privacy/rate-limit). ~23 tâches, ~6-8 semaines (confidence medium-high) |
| 0013 | [flowflow-contract: shared gate + FSM + canonicalization crate](./0013-shared-contract-crate/RFC.md) | Draft | — | Scindé de 0012: extraire le gate + FSM + canonicalisation en UN crate `flowflow-contract` (repo dédié), consommé par device + backend via git rev-pin + lockfiles + CI de drift cross-repo; le corpus de conformité de 0012 devient le test d'acceptation. Remplace la parité test-time de 0012 par une parité compile-time. Différé derrière 2 préalables durs prouvés non résolus par la revue de 0012: un spike build iOS (dépendance git privée via dx/appstore) et une CI qui existe vraiment. Pas sur le chemin critique d'un jalon produit. |
